# v0.33.21 — AI 사용량 카드 provider 마크 복원 RCA

## 분류

기준은 `main` 38da060b28ea6afe48139af026a13005197066d5 (v0.33.20)이다.

| # | 의도한 계약 | 확인한 현재 동작 | 판정 |
|---|---|---|---|
| 1 | 카드 제목 왼쪽에 provider 마크를 그린다 (v0.33.10, PR #196) | v0.33.19부터 마크가 없고 "Codex · 이름" 글자로만 provider를 표시한다 | hotfix (회귀) |
| 2 | provider 아이콘은 AI provider를 나타낸다 (v0.27.0, PR #149) | Cursor 아이콘이 일반 마우스 포인터 화살표라 마우스 커서로 읽힌다 | hotfix (표시 교정, v0.27.1 Codex 마크 교체와 같은 분류) |

## 1. 카드의 provider 마크가 사라짐

### 원인

- v0.33.10은 카드마다 14×14 `tk.Canvas`를 두고, 작업표시줄과 같은 도형 마크(`draw_provider_mark`)를
  제목 왼쪽에 그렸다.
- v0.33.19의 단일 Canvas 전환(f3793d4)은 카드의 네이티브 위젯을 모두 없애면서 이 마크 Canvas와
  테스트도 함께 지웠다. RCA와 릴리즈 노트에는 마크를 유지한다는 기록도, 뺀다는 기록도 없다.
  `_redraw_provider_mark`와 `draw_provider_mark` import는 쓰이지 않은 채 남았다.
- 마크 자체는 성능 원인이 아니었다. 그리는 비용은 도형 1~3개이고, v0.33.18의 렉은 카드마다
  네이티브 위젯을 두던 구조 전체에서 왔다(프로필 30개에서 위젯 601개 중 마크 Canvas 30개).

### 수정

- 제목줄은 v0.33.18과 같이 provider 마크와 프로필 이름으로 되돌린다. v0.33.19가 대신 넣었던
  "Provider · " 글자는 없앤다. provider는 카드 메뉴의 `제공자` 항목에서 계속 확인하고 바꿀 수 있다.
- 마크는 카드 영역(`CardRegion`)의 tag를 공유하는 Canvas 도형으로 그린다.
  - 카드 배치 때 한 번의 `canvas.move(tag)`가 마크도 옮긴다. 마크에 별도 좌표 호출이 없다.
  - 제목 첫 줄은 항상 카드 맨 위에서 시작하므로, 마크 위치는 다시 배치해도 바뀌지 않는다.
  - 제목 텍스트는 마크 폭만큼 오른쪽에서 시작하고, 그만큼 줄바꿈 폭이 줄어든다(`leading_width`).
  - 마크 크기는 12px을 최소로 두고 제목 글꼴 줄 높이의 0.7배를 따른다. 높은 DPI에서 제목과의 비율을 유지한다.
  - 마크를 눌러도 제목과 같이 선택·끌기가 시작되고, 우클릭은 카드 메뉴를 연다.
  - provider를 바꾸면 그 카드의 마크 항목만 지우고 다시 그린다.
- 카드당 네이티브 위젯은 늘지 않는다.

## 2. Cursor 아이콘이 마우스 포인터로 보임

### 원인

- v0.27.0(PR #149)이 작업표시줄 provider 아이콘을 넣을 때 Cursor는 일반 마우스 포인터 화살표로 그렸다.
  Codex는 v0.27.1(PR #151)에서 실제 브랜드 마크로 바뀌었지만 Cursor는 그대로 남았다.
- 그래서 Cursor 아이콘이 AI provider Cursor가 아니라 마우스 커서로 읽혔다. 카드 마크도 같은 그리기 함수를 쓴다.

### 수정

- Cursor의 큐브 마크(육각형에서 커서 모양 면을 파낸 모양)를 그린다.
  - 좌표는 Simple Icons `cursor.svg`(CC0 아이콘 데이터) 24px 경로의 선분 끝점을 10px 아이콘 상자로 옮긴
    값이다. 1px 미만의 모서리 곡선은 버렸다.
  - 마크는 Anysphere의 상표이며, provider를 표시하는 데만 쓴다.
- 파낸 면은 Codex의 `>_`처럼 바탕색으로 다시 칠한다.
- Cursor 마크는 단색이다. 바탕 밝기에 따라 어두운 작업표시줄 패널(#16181d)에서는 밝은 큐브(#f8fafc),
  흰 카드에서는 어두운 큐브(#111827)로 그린다.

## 검증

- targeted unit 테스트 493개 통과. 대상: `test_codex_usage_taskbar_overlay`, `test_codex_usage_ui`,
  `test_profile_board`, `test_profile_detail_canvas`, `test_main_ui_codex_layout`,
  `test_ai_usage_taskbar_panes` (모두 `tests.unit`).
  - 새로 넣거나 바꾼 테스트가 확인하는 내용:
    - 제목은 이름만 표시하고, provider별 마크의 도형·색·tag·위치가 맞다.
    - provider를 바꾸면 그 카드의 마크만 바뀐다.
    - 실제 Tk에서 폭을 1100/700/1100으로 바꿔도 마크가 제목 첫 줄 왼쪽에 있고, 위젯 수가 그대로다.
    - leading 폭만큼 제목이 밀리고 줄바꿈 폭이 줄어든다.
    - Cursor 큐브(꼭짓점 14개, 면 8개)가 바탕별 색으로 아이콘 상자 안에 그려진다.
- native QA (합성 데이터, 설치 없음):
  - 아이콘 캡처: 작업표시줄 패널 10px·40px, 흰 카드 12px·40px.
  - AI 사용량 탭, 프로필 6개(Codex·Cursor·Claude): 네이티브 위젯 44개, 마크 도형 12개, 콜백 오류 0.
  - v0.33.20과 번갈아 측정했다(프로필 30개, 5회씩).

  | 항목 | v0.33.20 | v0.33.21 |
  |---|---:|---:|
  | 네이티브 위젯 | 44 | 44 |
  | 최초 열기 중앙값 | 559.4ms | 582.9ms |
  | 다시 열기 중앙값 | 69.3ms | 79.8ms |
  | 크기 변경 중앙값 | 133.5ms | 132.6ms |
  | 연속 크기 변경 이벤트 간격 p95 (2회) | 30.1 / 29.5ms | 30.1 / 29.8ms |
  | 드래그 motion (2회) | 7.5 / 8.1ms | 6.1 / 7.9ms |

  - 최초 열기는 v0.33.20 안에서도 545~901ms로 흔들렸다. lane 전 시제품 측정(9회씩)에서는 593ms 대 589ms,
    다시 열기 76ms 대 70ms로 방향이 반대였다. 그래서 차이는 측정 편차 범위로 본다. 이 호스트의
    관측값이며 성능 보장이 아니다.
  - 드래그 회귀 스크립트가 누르는 지점은 마크 영역 안이며, 통과했다(프로필 HWND 1개).
- 전체 unit/E2E는 실행하지 않았다. 변경 호출 경로와 관계없는 기능으로 확대하지 않았다.
